/*

!What is Reverser Method ?
reverse() method in JavaScript arrays
The reverse() method is used to reverse the order of elements in an array.

Key thing to remember ⚠️
👉 It mutates (changes) the original array

!Basic syntax

array.reverse()

*/


//! Simple example

let arr = [1, 2, 3, 4, 5];
arr.reverse();
console.log(arr);

